apiVersion: v1
kind: ConfigMap
metadata:
  name: ds6-auth-service-config
  labels:
    app: ds6-auth-service
data:
  DB_HOST: "postgres-service"
  DB_PORT: "5432"
  DB_NAME: "distrischool_auth"
  JWT_EXPIRATION: "86400000"
